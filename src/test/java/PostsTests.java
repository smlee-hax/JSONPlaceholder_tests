//explicit imports to avoid bloat
import org.testng.Assert;
import org.testng.annotations.Test;

public class PostsTests {


    //POST

    //Success post (user, body, title)

    //Duplicate posts by same user (valid)

    @Test
    public void test1(){
        Assert.assertEquals(true, true);
    }



    //Assumption: is there a post limit. Some social media websites limit number of posts per minute to reduce spam
    //hit that post limit here


    //GET

}
